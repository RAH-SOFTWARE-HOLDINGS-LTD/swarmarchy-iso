alsa-firmware
base
base-devel
efibootmgr
linux-firmware
# aarch64: generic Arch Linux ARM kernel. Swap for a Snapdragon-X kernel
# (e.g. linux-x1e) once it is packaged for the mirror.
linux-aarch64
limine
openssh
pipewire
snapper
# Removed for aarch64:
#   amd-ucode, intel-ucode  -> x86 CPU microcode (n/a on ARM)
#   sof-firmware            -> Intel audio DSP firmware (n/a)
# TODO(aarch64): omarchy-keyring is x86-only; restore once an aarch64 omarchy repo exists.
